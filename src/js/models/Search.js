// build our data model with Classes 
//we descibe the data model for the search. The data is the query, search results 
export default class Search{
    constructor(query){
        this.query=query;
    }
}